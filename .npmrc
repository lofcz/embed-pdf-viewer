# Scope → public registry. Do NOT put _authToken here: an empty
# ${NPM_TOKEN} overrides interactive `npm login` and breaks `npm trust`.
# CI auth comes from actions/setup-node (registry-url) or a job env token.
@lofcz:registry=https://registry.npmjs.org/
@embedpdf:registry=https://registry.npmjs.org/
