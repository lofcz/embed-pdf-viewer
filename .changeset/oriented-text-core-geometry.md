---
'@embedpdf/core-geometry': minor
---

Adds semantic `TextQuad` geometry with corner-named transforms, bounds, rectangle conversion, positional PDF quad conversion, and resilient normalization for imported `/QuadPoints`.
