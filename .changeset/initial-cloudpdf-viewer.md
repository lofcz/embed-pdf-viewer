---
'@cloudpdf/viewer': major
---

Introduces the complete EmbedPDF viewer preconfigured for CloudPDF. It ships as a single CDN-ready `cloudpdf.js` artifact with no browser-side WASM or workers because document processing happens on the CloudPDF server.
