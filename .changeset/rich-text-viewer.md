---
'@embedpdf/viewer': minor
---

Expose `AnnotationFontSpec` and `AnnotationsCustomization` through the viewer entry points so applications can configure additional FreeText fonts with `annotations.fonts`.
