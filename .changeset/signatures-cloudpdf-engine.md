---
'@cloudpdf/engine': minor
---

Add `doc.signatures` for cloud-backed signature inspection, revision analysis, and two-phase signing. Read signed bytes from immutable document versions and refresh the manifest after preparing or completing a signature.

Support visual appearances on unsigned signature fields and publish their form update events.
