---
'@embedpdf/viewer-react': major
---

Introduces the React wrapper for the complete EmbedPDF viewer. `<PDFViewer>` configures the custom element while React children remain in the application's React tree and project into named viewer slots.
