---
'@embedpdf/plugin-stamp': minor
---

Publish a bundle-safe `/contract` entry for stamp-library tokens, assets, previews, configuration, and capability types without import, render, or plugin wiring.
