---
'@cloudpdf/contract': major
---

Introduces the CloudPDF engine contract package: the operation registry, zod schemas, and OpenAPI emitter for every backend-callable surface, used by `@cloudpdf/sdk` and `@cloudpdf/server` so client and server stay wire-compatible.
