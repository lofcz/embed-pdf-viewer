---
'@embedpdf/plugin-annotation': minor
---

Add `markupFromSelection()` for creating one oriented text-markup annotation per selected page and clearing the consumed selection. Multi-click draft finish and cancel actions are also available on the public annotation capability for composable menu controls.
