---
'@embedpdf/viewer': major
---

Introduces the complete EmbedPDF viewer as a custom element and CDN-ready artifact. It exposes `<embedpdf-viewer>` and `EmbedPDF.init()`, bundles the shared viewer chrome, and wires in the local WASM engine by default.
