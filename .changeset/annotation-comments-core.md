---
'@embedpdf/core': minor
---

Export `PermissionDenied` and add `DocumentsCapability.allows()` so hosts can
gate document-level print and download controls using the active document's
security policy.
