---
'@embedpdf/plugin-stamp': patch
---

Update canonical stamp-library import and append flows for the required
`pages.insert` and `pages.extract` engine operations, removing obsolete
optional-feature checks.
