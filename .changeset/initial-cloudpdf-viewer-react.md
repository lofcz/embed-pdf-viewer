---
'@cloudpdf/viewer-react': major
---

Introduces the React wrapper for the CloudPDF-powered full viewer. `<CloudPDFViewer>` accepts CloudPDF document tokens while retaining React-owned slots, context, and styling around the shared viewer component.
