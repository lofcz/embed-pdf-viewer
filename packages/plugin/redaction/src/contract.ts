/** Redaction capability protocol without destructive-apply plugin wiring. */
export { RedactionToken } from './types';
export type {
  RedactionCapability,
  RedactionLabelPatch,
  RedactionPendingItem,
  RedactionState,
} from './types';
export type { RedactionApplyResult } from '@embedpdf/engine-core';
