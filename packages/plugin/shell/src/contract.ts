/** Shell surface-state capability contract without reducer/plugin wiring. */
export { ShellToken } from './types';
export type {
  OpenSurfaceOptions,
  ShellAction,
  ShellCapability,
  ShellState,
  SurfaceState,
} from './types';
