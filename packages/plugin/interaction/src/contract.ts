/** Pointer/tool protocol and platform-feedback token without either plugin implementation. */
export { InteractionToken, samplePointOn } from './types';
export { FeedbackToken } from './feedback.types';
export type { FeedbackPluginOptions, PlatformFeedback } from './feedback.types';
export type {
  Cursor,
  InteractionAction,
  InteractionCapability,
  InteractionConfig,
  InteractionHandler,
  InteractionState,
  Modifiers,
  Phase,
  PointerKind,
  PointerSample,
  Tool,
  ToolId,
} from './types';
