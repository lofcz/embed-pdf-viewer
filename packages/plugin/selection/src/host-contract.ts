/** Host-typed selection token and gesture protocol for sibling plugins. */
export { SelectionToken } from './types';
export type {
  GlyphPointer,
  SelectionAction,
  SelectionHostCapability,
  SelectionRange,
  SelectionState,
} from './types';
