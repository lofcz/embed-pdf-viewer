/** View registry capability contract without reducer/plugin wiring. */
export { ViewManagerToken } from './types';
export type {
  View,
  ViewInfo,
  ViewManagerAction,
  ViewManagerCapability,
  ViewManagerState,
} from './types';
