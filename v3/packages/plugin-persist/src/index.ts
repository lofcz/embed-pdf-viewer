/**
 * @embedpdf/plugin-persist — effects-only plugin (requires Stage). Standard layout:
 * types.ts · effects.ts · persist.plugin.ts.
 */
export { persistPlugin } from './persist.plugin';
export type { PersistConfig } from './types';
