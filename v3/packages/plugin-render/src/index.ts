/**
 * @embedpdf-x/plugin-render — document-scoped render capability over the engine handle.
 * Standard layout: types.ts · capability.ts · render.plugin.ts.
 */
export { renderPlugin } from './render.plugin';
export { RenderToken } from './types';
export type { RenderCapability } from './types';
