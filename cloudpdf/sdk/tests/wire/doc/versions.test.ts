// This file was auto-generated by Fern from our API Definition.

import * as CloudPDF from "../../../src/api/index";
import { CloudPDFClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("VersionsClient", () => {
    test("list (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new CloudPDFClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            head: "head",
            versions: [
                {
                    sha256: "sha256",
                    byteLength: 1,
                    number: 1,
                    parentSha256: "parentSha256",
                    producer: "upload",
                    signingId: "signingId",
                    createdAt: 1,
                },
            ],
        };

        server
            .mockEndpoint()
            .get("/v1/docs/docId/versions")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.doc.versions.list({
            docId: "docId",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("list (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new CloudPDFClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v1/docs/docId/versions")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.doc.versions.list({
                docId: "docId",
            });
        }).rejects.toThrow(CloudPDF.NotFoundError);
    });

    test("analysis (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new CloudPDFClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            mode: "authoritative",
            policyVersion: 1,
            basis: { version: { sha256: "sha256", byteLength: 1 }, editsVersion: 1, source: "persisted" },
            since: { revisionIndex: 1, signatureIndex: 1 },
            until: { revisionIndex: 1 },
            restrictions: [
                {
                    signatureIndex: 1,
                    revisionIndex: 1,
                    source: "docmdp",
                    own: true,
                    permission: 1.1,
                    fields: { key: "value" },
                },
            ],
            current: {
                verdict: "unchanged",
                complete: true,
                primary: { rule: "rule", verdict: "permitted", objectNumber: 1, edge: "edge", detail: "detail" },
                findings: [{ rule: "rule", verdict: "permitted", objectNumber: 1 }],
                method: "net-state",
            },
            later: { revisionCount: 1, undoneObjectNumbers: [1] },
            verdict: "unchanged",
            steps: [{ key: "value" }],
        };

        server
            .mockEndpoint()
            .get("/v1/docs/docId/versions/analysis/sha")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.doc.versions.analysis({
            docId: "docId",
            sha: "sha",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("analysis (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new CloudPDFClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v1/docs/docId/versions/analysis/sha")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.doc.versions.analysis({
                docId: "docId",
                sha: "sha",
            });
        }).rejects.toThrow(CloudPDF.BadRequestError);
    });

    test("analysis (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new CloudPDFClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v1/docs/docId/versions/analysis/sha")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.doc.versions.analysis({
                docId: "docId",
                sha: "sha",
            });
        }).rejects.toThrow(CloudPDF.NotFoundError);
    });

    test("signatures (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new CloudPDFClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            chainValid: true,
            revisions: [{ index: 1, end: 1, xrefOffset: 1, signatureIndex: 1 }],
            signatures: [
                {
                    index: 1,
                    field: { kind: "objectNumber", fieldObjectNumber: 1 },
                    fieldName: "fieldName",
                    widget: { annotObjectNumber: 1, pageObjectNumber: 1 },
                    signed: true,
                    kind: "signature",
                    filter: "filter",
                    subFilter: "subFilter",
                    byteRange: [],
                    contentsSize: 1,
                    coverage: "whole-revision",
                    revisionIndex: 1,
                    signer: { name: null, reason: null, location: null, contactInfo: null, claimedTime: null },
                    docMdp: 1.1,
                    catalogCertification: true,
                    fieldMdp: { action: "all", fields: ["fields"] },
                    lock: { action: "all", fields: ["fields"] },
                    seedValue: {
                        requiredFlags: 1,
                        presentFlags: 1,
                        version: null,
                        mdp: null,
                        filter: null,
                        subFilters: ["subFilters"],
                        digestMethods: ["digestMethods"],
                        reasons: ["reasons"],
                        unsupportedRequired: true,
                    },
                },
            ],
            protection: {
                enforced: "none",
                judged: "none",
                certification: { signatureIndex: 1, permission: 1.1 },
                fieldLocks: [{ signatureIndex: 1, source: "fieldmdp", spec: null }],
                policyVersion: 1,
            },
        };

        server
            .mockEndpoint()
            .get("/v1/docs/docId/versions/signatures/sha")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.doc.versions.signatures({
            docId: "docId",
            sha: "sha",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("signatures (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new CloudPDFClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v1/docs/docId/versions/signatures/sha")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.doc.versions.signatures({
                docId: "docId",
                sha: "sha",
            });
        }).rejects.toThrow(CloudPDF.NotFoundError);
    });
});
