// This file was auto-generated by Fern from our API Definition.

import * as CloudPDF from "../../../src/api/index";
import { CloudPDFClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("SignaturesClient", () => {
    test("list (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new CloudPDFClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            chainValid: true,
            revisions: [{ index: 1, end: 1, xrefOffset: 1, signatureIndex: 1 }],
            signatures: [
                {
                    index: 1,
                    field: { kind: "objectNumber", fieldObjectNumber: 1 },
                    fieldName: "fieldName",
                    widget: { annotObjectNumber: 1, pageObjectNumber: 1 },
                    signed: true,
                    kind: "signature",
                    filter: "filter",
                    subFilter: "subFilter",
                    byteRange: [],
                    contentsSize: 1,
                    coverage: "whole-revision",
                    revisionIndex: 1,
                    signer: { name: null, reason: null, location: null, contactInfo: null, claimedTime: null },
                    docMdp: 1.1,
                    catalogCertification: true,
                    fieldMdp: { action: "all", fields: ["fields"] },
                    lock: { action: "all", fields: ["fields"] },
                    seedValue: {
                        requiredFlags: 1,
                        presentFlags: 1,
                        version: null,
                        mdp: null,
                        filter: null,
                        subFilters: ["subFilters"],
                        digestMethods: ["digestMethods"],
                        reasons: ["reasons"],
                        unsupportedRequired: true,
                    },
                },
            ],
            protection: {
                enforced: "none",
                judged: "none",
                certification: { signatureIndex: 1, permission: 1.1 },
                fieldLocks: [{ signatureIndex: 1, source: "fieldmdp", spec: null }],
                policyVersion: 1,
            },
        };

        server
            .mockEndpoint()
            .get("/v1/docs/docId/layers/layerName/signatures")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.doc.signatures.list({
            docId: "docId",
            layerName: "layerName",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("list (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new CloudPDFClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v1/docs/docId/layers/layerName/signatures")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.doc.signatures.list({
                docId: "docId",
                layerName: "layerName",
            });
        }).rejects.toThrow(CloudPDF.NotFoundError);
    });

    test("abort (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new CloudPDFClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = { status: "aborted" };

        server
            .mockEndpoint()
            .delete("/v1/docs/docId/layers/layerName/signatures/signingId")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.doc.signatures.abort({
            docId: "docId",
            layerName: "layerName",
            signingId: "signingId",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("abort (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new CloudPDFClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .delete("/v1/docs/docId/layers/layerName/signatures/signingId")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.doc.signatures.abort({
                docId: "docId",
                layerName: "layerName",
                signingId: "signingId",
            });
        }).rejects.toThrow(CloudPDF.NotFoundError);
    });

    test("complete (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new CloudPDFClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { cms: "cms", expectedVersion: { baseSha256: "baseSha256", editsVersion: 1 } };
        const rawResponseBody = {
            status: "completed",
            signature: {
                index: 1,
                field: { kind: "objectNumber", fieldObjectNumber: 1 },
                fieldName: "fieldName",
                widget: { annotObjectNumber: 1, pageObjectNumber: 1 },
                signed: true,
                kind: "signature",
                filter: "filter",
                subFilter: "subFilter",
                byteRange: [{ key: "value" }],
                contentsSize: 1,
                coverage: "whole-revision",
                revisionIndex: 1,
                signer: {
                    name: "name",
                    reason: "reason",
                    location: "location",
                    contactInfo: "contactInfo",
                    claimedTime: "claimedTime",
                },
                docMdp: 1.1,
                catalogCertification: true,
                fieldMdp: { action: "all", fields: ["fields"], permission: 1.1 },
                lock: { action: "all", fields: ["fields"], permission: 1.1 },
                seedValue: {
                    requiredFlags: 1,
                    presentFlags: 1,
                    version: 1,
                    mdp: 1.1,
                    filter: "filter",
                    subFilters: ["subFilters"],
                    digestMethods: ["digestMethods"],
                    reasons: ["reasons"],
                    unsupportedRequired: true,
                },
            },
            version: { sha256: "sha256", byteLength: 1 },
            previous: { baseSha256: "baseSha256", editsVersion: 1 },
            protection: {
                enforced: "none",
                judged: "none",
                certification: { signatureIndex: 1, permission: 1.1 },
                fieldLocks: [{ signatureIndex: 1, source: "fieldmdp", spec: null }],
                policyVersion: 1,
            },
            meta: {
                affectedPages: [
                    {
                        pageObjectNumber: 1,
                        revision: { docSessionId: "docSessionId", pageObjectNumber: 1, generation: 1 },
                        weakAnnotationState: { kind: "unknown" },
                    },
                ],
                cacheDelta: {
                    previousDocVersion: 1,
                    docVersion: 1,
                    annotationsVersion: 1,
                    layerVersion: 1,
                    working: true,
                    pages: [{ pageObjectNumber: 1, cache: { contentVersion: 1, annotationVersion: 1 } }],
                },
            },
        };

        server
            .mockEndpoint()
            .post("/v1/docs/docId/layers/layerName/signatures/signingId/complete")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.doc.signatures.complete({
            docId: "docId",
            layerName: "layerName",
            signingId: "signingId",
            cms: "cms",
            expectedVersion: {
                baseSha256: "baseSha256",
                editsVersion: 1,
            },
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("complete (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new CloudPDFClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { cms: "cms", expectedVersion: { baseSha256: "baseSha256", editsVersion: 1 } };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v1/docs/docId/layers/layerName/signatures/signingId/complete")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.doc.signatures.complete({
                docId: "docId",
                layerName: "layerName",
                signingId: "signingId",
                cms: "cms",
                expectedVersion: {
                    baseSha256: "baseSha256",
                    editsVersion: 1,
                },
            });
        }).rejects.toThrow(CloudPDF.BadRequestError);
    });

    test("complete (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new CloudPDFClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { cms: "cms", expectedVersion: { baseSha256: "baseSha256", editsVersion: 1 } };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v1/docs/docId/layers/layerName/signatures/signingId/complete")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.doc.signatures.complete({
                docId: "docId",
                layerName: "layerName",
                signingId: "signingId",
                cms: "cms",
                expectedVersion: {
                    baseSha256: "baseSha256",
                    editsVersion: 1,
                },
            });
        }).rejects.toThrow(CloudPDF.NotFoundError);
    });

    test("complete (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new CloudPDFClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { cms: "cms", expectedVersion: { baseSha256: "baseSha256", editsVersion: 1 } };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v1/docs/docId/layers/layerName/signatures/signingId/complete")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(409)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.doc.signatures.complete({
                docId: "docId",
                layerName: "layerName",
                signingId: "signingId",
                cms: "cms",
                expectedVersion: {
                    baseSha256: "baseSha256",
                    editsVersion: 1,
                },
            });
        }).rejects.toThrow(CloudPDF.ConflictError);
    });

    test("analysis (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new CloudPDFClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            mode: "authoritative",
            policyVersion: 1,
            basis: { version: { sha256: "sha256", byteLength: 1 }, editsVersion: 1, source: "persisted" },
            since: { revisionIndex: 1, signatureIndex: 1 },
            until: { revisionIndex: 1 },
            restrictions: [
                {
                    signatureIndex: 1,
                    revisionIndex: 1,
                    source: "docmdp",
                    own: true,
                    permission: 1.1,
                    fields: { key: "value" },
                },
            ],
            current: {
                verdict: "unchanged",
                complete: true,
                primary: { rule: "rule", verdict: "permitted", objectNumber: 1, edge: "edge", detail: "detail" },
                findings: [{ rule: "rule", verdict: "permitted", objectNumber: 1 }],
                method: "net-state",
            },
            later: { revisionCount: 1, undoneObjectNumbers: [1] },
            verdict: "unchanged",
            steps: [{ key: "value" }],
        };

        server
            .mockEndpoint()
            .get("/v1/docs/docId/layers/layerName/signatures/analysis")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.doc.signatures.analysis({
            docId: "docId",
            layerName: "layerName",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("analysis (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new CloudPDFClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v1/docs/docId/layers/layerName/signatures/analysis")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.doc.signatures.analysis({
                docId: "docId",
                layerName: "layerName",
            });
        }).rejects.toThrow(CloudPDF.BadRequestError);
    });

    test("analysis (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new CloudPDFClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v1/docs/docId/layers/layerName/signatures/analysis")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.doc.signatures.analysis({
                docId: "docId",
                layerName: "layerName",
            });
        }).rejects.toThrow(CloudPDF.NotFoundError);
    });
});
